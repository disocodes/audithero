# AuditHero Fabric Environment

Production target: Fabric Runtime 2.0. `Sparkcompute.yml` must use `runtime_version: "2.0"`. The deployer uploads the shared AuditHero wheel as a custom library and publishes the Environment before running notebooks.