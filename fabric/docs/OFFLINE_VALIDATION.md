# Validation environments

The repository should be validated in GitHub/Databricks/Fabric runtimes. Local container environments without internet access cannot pull the repository or call platform APIs; this does not replace the platform-native self-tests.