# Complete Databricks engine

`schads_audit.databricks_pipeline_v2.run_databricks_audit_v2` is the parity execution path that applies every shared compliance module before reconciliation. Existing Databricks jobs are being redirected to this path as part of the complete-platform release.