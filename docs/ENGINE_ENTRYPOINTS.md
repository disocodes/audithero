# Engine entry points

Fabric: `schads_audit.fabric_pipeline.run_fabric_audit`.

Databricks: `schads_audit.databricks_pipeline_v2.run_databricks_audit_v2`.

Both apply the same shared calculation modules in the same order before reconciliation.