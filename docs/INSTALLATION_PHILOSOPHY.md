# Installation philosophy

Both target platforms should be deployable from a clean workspace without copying calculation cells manually. Platform-native resources are created from source control, while tenant credentials and historical evidence remain environment-specific.