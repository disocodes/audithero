# Data retention

Payroll audit evidence should be retained in accordance with applicable employment/payroll recordkeeping, privacy and organisational policies. AuditHero keeps run-scoped append-only outputs by `audit_run_id`; platform administrators should configure retention/backup controls rather than deleting prior audit runs merely because a newer snapshot exists.