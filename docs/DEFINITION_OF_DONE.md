# Definition of done

A platform target is complete when a fresh workspace can be deployed from Git, self-tested, connected to Employment Hero, readiness-scanned, run historically for parameterised dates, run on a recurring payroll schedule, and present results through its native BI layer without editing calculation code in the UI.