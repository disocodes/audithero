# Testing layers

AuditHero has three test layers: platform-independent Python regression tests, Databricks-native self-tests, and Fabric-native self-tests. Production acceptance additionally requires reconciliation against known payroll examples.