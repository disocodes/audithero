# Build status criteria

The `complete-both-platforms` branch is intended to be merged only after Fabric deployment automation and Databricks compliance-module parity are both present and runtime self-test entry points are defined.